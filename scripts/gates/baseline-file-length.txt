# baseline-file-length — 1500줄 초과 파일의 봉인 줄 수. 이 파일은 봉인이지 해소가 아니다.
# 갱신은 --prune(축소)만. 값을 올리는 것은 명시 재입법 커밋으로만 한다(C5 — 무언 완화 금지).
# 제거 조건: 파일을 1500줄 이하로 분할 → stale 실패 → --prune 으로 항목이 삭제된다.
# 파일 개명 시 같은 커밋에서 항목 경로를 수기로 이전한다(값 불변).
#
# 재입법 이력(C5 — 무언 완화 아님, 명시 커밋):
#   2026-07-12 — 터미널 seam 계약화 + plugin-spec 0.3.0 이 4파일을 봉인 초과로 키움. 각 증가분은
#     필요 기능이라 조용히 축소 불가: spec.ts 신규 봉인(0.3.0 viewContract/status 필드 — 단일진실
#     spec 이라 분할이 오히려 단일진실을 훼손), catalog.ts 2715→2739(계약 해소 layout skip),
#     i18n.ts 1525→1531(엔진 0 skip 메시지 — 번역 콘텐츠라 축소 불가), sessions.ts 1745→1755
#     (계약 뷰 해소 newViewFor). 1500 이하 분할-축소는 별도 부채 정리 패스(이 세션 범위 밖).
#   2026-07-12 — 터미널 미러 코어 방출 브랜치를 main 에 병합. 두 재입법이 합류: api.ts 1784→1816
#     (방출 소비자 배선 — paneAlive 세션-생존 조회, 코어 무해석 유지), spec.ts 1503→1523(양쪽이
#     같은 viewContract 필드를 독립 추가 → 병합에서 중복 선언 제거 후 단일 계약-핀으로 수렴). 분할은 부채 패스.
#   2026-07-13 — 사이드카 유닛 선택의 단일진실. api.ts 1816→1849: 매니페스트가 유닛을 고른다는
#     계약을 코어가 강제하게 됐다 — 플러그인이 자기 선언을 읽는 표면(sidecarName)과, 선언되지 않은
#     유닛의 스폰을 거부하는 게이트. 이전엔 스폰 경로에 대조가 없어 번들 상수가 매니페스트를 이겼고,
#     선언만 바꾼 교체가 무음으로 옛 엔진을 띄웠다. 게이트를 지우면 그 결함이 돌아오므로 축소 불가.
#   2026-07-13 — 계약-핀의 소비자 축(C4 재입법). 발견은 계약으로 하는데 호출은 이름으로 해야 했다:
#     cross-plugin 게이트가 manifest.dependencies(구현체 id)만 인정해, 계약으로 구현체를 찾아도
#     부를 수가 없었다 — L2 계약-핀의 약속이 런타임에서 거짓이었다. manifest.consumes(계약 id) 를
#     implements 의 대칭 축으로 신설하고 게이트가 그것을 인정한다. spec.ts 1523→1533(필드·검증),
#     api.ts 1849→1877(게이트의 두 축·주입면). 경계는 그대로다 — 무선언 호출은 여전히 거부된다.
#   2026-07-13 — 실패 응답의 사람 줄이 엔진 방언을 나르지 않는다. 예외 경로가 던져진 원문을 그대로
#     message 로 올려, 저장소가 낸 "out of memory" 가 "앱이 죽었다" 로 읽혔다(실측). 원문은 data.detail
#     로 내리고 사람 줄은 문장으로 짓는다 — i18n.ts 1531→1533(키 1개, ko/en 두 테이블). 언어 테이블은
#     기능이 늘면 함께 자라는 표라 이 증가는 분할 대상이 아니다.
#   2026-07-13 — 저장소가 자기 무결성을 말하고 고칠 수 있게 됐다(data.verify·data.repair). 부팅 게이트는
#     quick_check 라 인덱스↔테이블 대조를 하지 않아 인덱스 손상을 통과시킨다 — 그 저장소는 읽기는 멀쩡하고
#     쓰기만 무너진다(실측). i18n.ts 1533→1541(키 4개, ko/en 두 테이블). 언어 테이블은 기능이 늘면 함께 자라는 표다.
#   2026-07-13 — 저장소 실황 표면(data.stats) — 앱 안의 SQLite 가 자기 한도·메모리를 답한다. `out of memory`
#     의 원인은 프로세스 밖에서 가릴 수 없다. i18n.ts 1545→1549(키 2개, ko/en).
#   2026-07-13 — ns 회수 표면(data.ns.remove). 만드는 길만 있고 걷는 길이 없어 시험이 남의 저장소에 흔적을
#     남겼다(회수 3축의 데이터 축 구멍). i18n.ts 1541→1545(키 2개, ko/en).
#
# ── 2026-07-29 명시 재입법 ────────────────────────────────────────────────────
# 아래 항목들의 값을 올린다. 사유를 남긴다 — 올린 근거가 없는 봉인은 곧 아무 뜻이 없다.
#
# 원인 ①(다수): 이 게이트가 쓰는 `stripRustTestModule` 이 중괄호 없는 `#[cfg(test)] mod x;`
#   를 블록으로 착각해 **다음에 나오는 아무 블록이나 삼키고 있었다**(같은 커밋에서 고침).
#   그래서 실행 경로의 unwrap 이 스캔 밖에 있었고, 봉인은 그 눈먼 수를 적고 있었다.
#   실측: data/mod.rs 는 실행 경로 21건인데 1로 봉인돼 있었고, process.rs 는 실제 0건인데
#   (7건 전부 검사 안) 5로 봉인돼 있었다. 새 수가 진실이다.
# 원인 ②: 그동안 게이트가 RED 인 채 방치됐다. 확인이 출력 문자열 대조라 이 게이트의 실패를
#   놓쳤다(이 게이트는 "FAIL" 이라는 낱말을 안 쓴다). scripts/gates/run-all.mjs 가 그 구멍을 막는다.
#
# 이것은 허용이 아니라 **기록**이다. 값을 내리는 길은 그대로다 — 고치고 --prune.
#
# ── 2026-07-29 명시 재입법 ────────────────────────────────────────────────────
# 이 날 16개 항목의 값이 올랐다. 사유를 남긴다 — 올린 근거가 없는 봉인은 곧 아무 뜻이 없다.
#
# 원인 ① 이 게이트가 쓰는 stripRustTestModule 이 중괄호 없는 `#[cfg(test)] mod x;` 를 블록으로
#   착각해 다음에 나오는 아무 블록이나 삼키고 있었다(같은 날 고침). 실행 경로의 unwrap 이
#   스캔 밖이었고 봉인은 그 눈먼 수를 적고 있었다. 실측: data/mod.rs 는 실행 경로 21건인데
#   1로 봉인돼 있었고, process.rs 는 실제 0건(7건 전부 검사 안)인데 5로 봉인돼 있었다.
# 원인 ② 그동안 이 게이트가 RED 인 채 방치됐다. 확인이 출력 문자열 대조라 실패를 놓쳤다 —
#   이 게이트는 "FAIL" 이라는 낱말을 쓰지 않는다. scripts/gates/run-all.mjs 가 그 구멍을 막는다.
#
# 이것은 허용이 아니라 **기록**이다. 내리는 길은 그대로다 — 고치고 --prune.
#
# ── 2026-07-29 뿌리 확장 재입법 ───────────────────────────────────────────────
# crates/* 가 이 게이트의 스캔 뿌리에 없었다. 그래서 코어·store·cored·ptyd 는 한 번도
# 세어진 적이 없다(unwrap 49건 · 1500줄 초과 2파일). 이관 목적지가 스캔 밖이면 프레임워크
# 에서 옮긴 파일의 부채가 옮겨지는 순간 영영 안 세어진다 — 뿌리 확장이 이관보다 먼저인 이유다.
# 아래 항목은 새로 생긴 부채가 아니라 **처음 보이게 된 부채**다.
#
# ── 2026-07-29 UNSERVED 사유 감사 재입법 ──────────────────────────────────────
# registry.rs 2408→2469. 늘어난 61줄은 전부 `UNSERVED` 의 `blocked_by` 산문이고, 코드는
# 한 줄도 늘지 않았다. 사유 감사에서 세 무리가 **사실과 반대**였다:
#   secret_status — "이 프로세스는 볼트를 열지 않는다"였는데 그 명령은 볼트를 **만든다**
#     (status → keys → is_unlocked → ensure_open → KEK get-or-create + flush).
#   볼트 11건 — 몸이 soksak-vault 로 나가고 KEK 주입 이음매가 공개가 된 뒤로 "볼트 자체가
#     벽"이 아니게 됐다. 남은 벽(키체인 신원 ACL·읽기가 쓰기·expect_vault 깃발)을 새로 적었다.
#   ipc_last_project_window — "cored 는 그 사건을 받지 않는다"가 control_host_attach·
#     control_windows 로 해소됐다. 남은 벽은 보고하지 않는 프레임워크 쪽이다.
# 틀린 사유는 없느니만 못하다 — 옮길 수 있는 것을 못 옮긴다고 적으면 그 이름은 다시 조사되지
# 않는다. 줄이는 길은 그대로다: 이 표를 별도 파일로 갈라내면 --prune 이 항목을 지운다.
crates/soksak-cli/src/lib.rs 2095
crates/soksak-cored/src/registry.rs 2469
crates/soksak-store/src/store.rs 2115
frameworks/tauri/src/ipc.rs 1523
frameworks/tauri/src/plugins.rs 1585
frameworks/tauri/src/schedule.rs 1624
frameworks/tauri/src/webview.rs 2259
packages/plugin-spec/src/pluginRuntime.ts 2081
packages/plugin-spec/src/spec.ts 1783
src/commands/catalog.ts 3284
src/i18n.ts 1811
src/plugins/api.ts 1926
src/state/sessions.ts 2006
