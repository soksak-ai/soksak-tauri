{
  "spec": "soksak-spec-plugin@1",
  "id": "valid-implements",
  "name": "Valid Implements Fixture",
  "version": "0.1.0",
  "description": "headless gate fixture — implements contract pins must pass parseManifest",
  "permissions": [],
  "implements": ["soksak-spec-plugin-fixture-tasks@1"]
}
