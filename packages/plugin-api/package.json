{
  "name": "@soksak-ai/plugin-api",
  "version": "0.2.1",
  "description": "soksak plugin runtime API contract types (ctx.app, Plugin, PluginContext) — the @types/editor equivalent for soksak.",
  "license": "MIT",
  "author": "soksak-ai",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/soksak-ai/soksak.git",
    "directory": "packages/plugin-api"
  },
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "node -e \"if(!/pnpm/.test(process.env.npm_config_user_agent||''))throw new Error('workspace: 의존성은 pnpm publish 로만 발행합니다 — npm publish 는 깨진 패키지를 만듭니다(0.2.0 실측)')\" && pnpm run build"
  },
  "dependencies": {
    "@soksak-ai/plugin-spec": "workspace:^"
  },
  "publishConfig": {
    "access": "public"
  }
}
