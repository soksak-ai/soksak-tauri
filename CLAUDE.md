## R1 문제를 문제로 해결하지 않는다

- 증상 위에 우회 레이어를 쌓지 않는다.
- 불안정한 현상을 그대로 기준으로 삼지 않는다.
- 원인은 규칙, 계약, 상태 소유권, 인터페이스 경계에서 찾아 고친다.
- 레거시 보완은 새 구조의 일부로 설계한다.
- 임시 회피가 필요하면 임시임을 문서화하고 제거 조건과 검증 방법을 둔다.

## R2 모든 변경은 RED->GREEN으로 증명한다

- 결함 수정과 기능 추가는 실패하는 재현 테스트에서 시작한다.
- RED는 목표 결함 때문에 실패해야 한다.
- GREEN은 RED에서 세운 같은 기준으로 확인한다.
- 테스트 없는 완료 선언은 금지다.
- 테스트가 어려우면 먼저 검증 가능한 커맨드, 상태, 인터페이스를 만든다.

## R3 UI는 시각 검증한다

- UI 변경은 스크린샷을 직접 보고 확인한다.
- 자동 테스트가 통과해도 화면이 깨지면 완료가 아니다.
- 검증에 필요한 명령이나 노출면이 없으면 먼저 만든다.
- 캡처에서 발견한 결함은 수정 후 다시 캡처한다.

## R4 기준 약화 금지

- 테스트가 실패한다고 기준을 낮추지 않는다.
- 기준이 맞으면 구현, fixture, 문서, 노출 API를 고친다.
- 기준 자체가 틀렸다면 명시적으로 문제 제기하고 새 기준을 세운다.
- "합리적 절충"이라는 말로 품질 기준 붕괴를 정당화하지 않는다.

## R5 확정사항 재질문 금지

- 이미 결정된 사항을 다시 묻지 않는다.
- 명확한 요구를 선택지로 되돌리지 않는다.
- 새 사실 때문에 기존 결정 변경이 필요할 때만 질문한다.
- 질문할 때는 충돌한 기준, 필요한 결정, 영향을 구체적으로 적는다.
- 질문이 필요 없으면 올바른 방향으로 실행한다.

## R6 원칙 먼저, 코드 나중

- 구현 전에 적용할 규칙과 계약을 분명히 한다.
- 규칙은 코드, 테스트, 문서 중 적절한 위치에 남긴다.
- 구현은 규칙의 시행이어야 한다.
- 우연한 레거시 상태를 원칙처럼 따르지 않는다.

## R7 강결합 금지, 인터페이스 우선

- 코어, 모듈, 플러그인, UI, 자동화 경계는 공개 인터페이스로 연결한다.
- 내부 상태, private DOM, 파일 위치, 로딩 순서에 기대지 않는다.
- 필요한 동작은 발견 가능한 커맨드나 API로 노출한다.
- 필요한 상태는 읽을 수 있는 상태면으로 노출한다.
- 필요한 이벤트는 추적 가능한 경로로 흐르게 한다.

## 경로와 자동화

- symlink 금지.
- 상대경로 추측에 기대지 않는다.
- 경로는 선언적 설정이나 발견 가능한 규칙으로 해석한다.
- 임시 스크립트 금지.
- 반복 가능한 작업은 재사용 가능하고 멱등한 명령, 테스트, 스크립트로 만든다.

## 이벤트와 폴링

- 폴링은 최후 수단이다.
- 먼저 이벤트, 구독, watcher, callback, 명시적 상태 변경 알림을 찾거나 만든다.
- 폴링이 불가피하면 이유, 주기, 종료 조건, 백오프, 테스트를 남긴다.
- 무한 루프성 감시는 금지다.

## 질문 기준

질문 전 다음을 점검한다.

1. 정말 필요한 질문인가.
2. 이미 결정되거나 명확한 것을 되묻는가.
3. 새 이정표 때문에 기존 결정을 바꿔야 하는가.
4. 습관적으로 선택지를 주려는가.
5. 질문이 누락이나 혼란을 만들지 않는가.
6. 꼭 필요한 질문을 누락하고 있지는 않은가.

통과하지 못하면 질문하지 않는다. 실행한다.

## 완료 선언 전 검증

- RED와 GREEN 결과를 확인한다.
- UI 변경이면 스크린샷을 확인한다.
- 노출해야 할 커맨드, 상태, 이벤트, 문서가 빠졌는지 확인한다.
- 폴링, symlink, 임시 스크립트, 강결합이 들어갔는지 확인한다.
- 기준을 낮춘 부분이 없는지 확인한다.
- 변경 파일을 확인한다.

검증하지 않은 완료 선언은 금지다.
