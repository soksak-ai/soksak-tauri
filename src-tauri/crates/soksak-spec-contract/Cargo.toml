[package]
name = "soksak-spec-contract"
version = "0.0.1"
edition = "2021"
publish = false
license = "MIT"
repository = "https://github.com/soksak-ai/soksak-spec"
description = "Public soksak contract provider/consumer references and deterministic SemVer range matching shared by core, sidecars, services, and SDK tooling."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
