// 기여는 매니페스트 선언이 전부(programs 는 자동 등록) — 코드 없음.
// 실행/설치 명령도 plugin.json 에 선언돼 동의 화면에 원문 그대로 고지된다.
//
// 공식 설치법 출처(2026-06): https://code.claude.com/docs/en/setup

export default {
  activate() {},
  deactivate() {},
};
