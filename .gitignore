# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 산출물(추적 제외). 플러그인은 영구 미러 없이 make plugin-publish(임시 clone)로 발행 —
# plugins/.repos* 는 구 plugin-repos 산출물(폐기). .repos_bak 은 삭제 전 백업.
plugins/.repos*
examples/.repos/

# apphost Swift 사이드카 컴파일 산출물(빌드 파이프라인 생성, Tauri externalBin) — 플랫폼별 재생성
src-tauri/binaries/

# Editor directories and files
.editor/*
!.editor/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# perf 측정 하니스의 컴파일 산출물(synth-input.swift 에서 재생성)
scripts/perf/.build/

secret/
