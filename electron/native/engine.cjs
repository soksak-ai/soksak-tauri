// engine_* — 사이드카 엔진(CEF)이 창에 붙이는 호스트 뷰·서피스. 붙는 자리가 창이라 프레임워크의 것이다.

module.exports = {
  // 호스트 뷰의 네이티브 포인터 — 사이드카가 자기 서피스를 그 위에 얹을 자리를 받는다.
  // 원본은 창 레이어 맵에서 host_ptr 을 꺼낸다(macOS 전용).
  engine_host_ptr: {
    concept: "엔진 호스트 뷰 포인터",
    absent:
      "얹을 호스트 뷰가 없다 — 사이드카 표면을 창에 붙이는 배선이 없어 가리킬 포인터도 없다. 0 을 돌려주면 유효한 주소로 읽혀 사이드카가 그 위에 얹으려 한다.",
  },

  // 서피스 숨김 — 오버레이 동안 사이드카 서피스를 가린다. 가릴 서피스가 없다.
  engine_surface_hide: {
    concept: "엔진 서피스 숨김",
    absent:
      "가릴 서피스가 없다 — 콘텐츠가 DOM 안에 살아 겹침이 z-index 로 해결되므로 오버레이 동안 무엇을 숨길 필요가 없다.",
  },

  // 적재된 엔진 모듈을 한 바퀴 돌려 살아 있게 한다(CEF 메시지 루프). 적재된 모듈이 없다.
  engine_tick_all: {
    concept: "엔진 모듈 틱",
    absent:
      "돌릴 엔진 모듈이 없다 — 이 프레임워크는 통째로 Chromium 이라 콘텐츠를 그리려고 CEF 를 적재하지 않는다(docs/FRAMEWORK-PORT §왜 둘인가).",
  },

  // CEF 엔진 호스트 뷰의 숨김/복귀. 이 프레임워크엔 사이드카 표면을 창에 붙이는 배선이 없어 호스트가 없다.
  engine_host_visible: {
    concept: "엔진 호스트 뷰 가시성",
    absent: "이 프레임워크엔 엔진 호스트 뷰가 없다 — 사이드카 표면을 창에 붙이는 배선이 없다.",
  },

  // 엔진 서피스 실측(가시성·frame). 이 프레임워크는 엔진 서피스를 등록받지 않으므로 셀 대상이 없는 게
  // 아니라 **볼 눈이 없다**. registered:0 을 돌려주면 재지 않은 것을 쟀다고 말하게 되고, 표면
  // 감사는 그 0 으로 "보여야 할 것이 안 보인다"는 없는 결함을 발행한다.
  engine_surface_stats: {
    concept: "엔진 서피스 실측",
    absent:
      "이 프레임워크는 엔진 서피스를 등록받지 않는다 — 0 을 돌려주면 재지 않은 것을 쟀다고 말하게 되고, 표면 감사가 없는 결함을 발행한다.",
  },
};
