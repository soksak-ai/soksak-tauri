//! 프로젝트 root 점유의 **사건 이름** — 지도를 가진 구현이 둘이라 여기서 정한다.
//!
//! 지도는 창을 소유한 쪽이 진다(수명이 창의 수명과 같다). 그래서 구현이 프레임워크마다 하나씩
//! 있고, 규칙이 갈리지 않는다는 것은 `fixtures/project-claims.json` 이 묶는다. 이름은 그 규칙의
//! 일부인데 픽스처 밖에 있었다 — 리터럴로 흩어지면 한쪽 발행이 아무에게도 안 닿고, 그 부재는
//! 오류가 아니라 **갱신 안 되는 화면**이다(다른 창의 프로젝트 픽커가 낡은 목록을 계속 보인다).
//!
//! 실측(2026-08-01): Electron 쪽 `project.cjs` 에는 발행이 **아예 없었다**. 그 프레임워크에서
//! 프로젝트를 점유·해제하면 같은 프로세스의 다른 창도 그 사실을 몰랐다.

/// 지도가 바뀌었다 — 창들이 자기 목록을 다시 읽는다.
pub const CHANGE_EVENT: &str = "project-registry-change";
