//! 바깥으로 나가는 바이트 — 전송기 한 벌과 그 위에 선 둘.
//!
//! 여기 있는 것은 창을 한 번도 부르지 않는다. 그러나 코어에도 못 산다: 전송기가 wreq 하나이고
//! wreq 는 tokio 를 끌고 오는데, tokio 는 soksak-core 의 무의존과 soksak-cored 의 no_framework
//! 금지 목록 양쪽에 걸린다. 그래서 이 크레이트가 그 자원을 **혼자** 진다 — 프레임워크가 둘이든
//! 셋이든 전송 규칙은 여기 한 벌이고, 그것을 부르는 명령 래퍼만 각 프레임워크에 남는다.
//!
//! 앰비언트를 안 읽는다. 열린 볼트도(해소 함수를 인자로 받는다), 기동한 프록시의 포트·토큰도
//! (`MediaProxy` 손잡이가 들고 있다) 전역이 아니다. 전역이면 같은 코드가 프로세스마다 다른
//! 답을 내고, 그 차이는 오류가 아니라 "저쪽에서만 재생이 안 된다"로 나타난다.

pub mod http;
pub mod mediaproxy;
pub mod transport;
